import XCTest
@testable import Lambton_EasyRent

final class Lambton_EasyRentTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(Lambton_EasyRent().text, "Hello, World!")
    }
}
